let gameOSnake = function snakeGame(doInstruct) {
    console.log('type emacs and hit enter. Wait for text appear then hold down ESC and press x. Cursor has moved to the bottom of the page next to the letters M-x. Now type in snake... YOUR WELCOME!');
  };

gameOSnake();
